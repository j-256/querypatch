{
  "name": "querypatch",
  "version": "1.0.0",
  "private": true,
  "description": "A compact Chrome extension for query string rewrite rules",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=20"
  }
}
