{
  "name": "querypatch",
  "version": "1.0.0",
  "private": true,
  "description": "A compact Chrome extension for pattern-based query parameter rewriting with a built-in URL tester",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/j-256/querypatch.git"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test",
    "capture:cover": "node scripts/capture-cover.mjs"
  },
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
